"use client";
import {FormEvent,useState} from "react";

export default function Login(){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(r.ok){location.href="/dashboard";return}const body=await r.json().catch(()=>({error:"Não foi possível entrar."}));setError(body.error);setLoading(false)}return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span>T</span><b>TapLink</b></a><small>PAINEL DO ESTABELECIMENTO</small><h1>Bem-vindo de volta.</h1><p>Entre para editar sua página, seus links e acompanhar resultados.</p><form onSubmit={submit}><label>E-mail<input name="email" type="email" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" minLength={10} required/></label>{error&&<div className="form-error">{error}</div>}<button disabled={loading}>{loading?"Entrando…":"Entrar no painel"}</button></form><footer>Ambiente proprietário · acesso autorizado</footer></section><aside><small>TUDO EM UM TOQUE</small><h2>Um painel.<br/>Múltiplos negócios.</h2><p>Personalização white-label com isolamento real entre empresas.</p></aside></main>}
