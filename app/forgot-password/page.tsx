"use client";
import { FormEvent, useState } from "react";
export default function ForgotPassword(){const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/auth/password-reset/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const body=await response.json();setMessage(body.message??"Se o e-mail estiver cadastrado, enviaremos as instruções.");setLoading(false)}return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span>T</span><b>TapLink</b></a><small>RECUPERAÇÃO SEGURA</small><h1>Esqueceu a senha?</h1><p>Enviaremos um link de uso único, válido por 30 minutos.</p><form onSubmit={submit}><label>E-mail<input name="email" type="email" autoComplete="email" required/></label>{message&&<div className="form-success">{message}</div>}<button disabled={loading}>{loading?"Solicitando…":"Enviar instruções"}</button></form><footer><a href="/login">Voltar ao login</a></footer></section><aside><small>SEM ATALHOS PERIGOSOS</small><h2>Token único.<br/>Sessões revogadas.</h2><p>A resposta não revela se uma conta existe.</p></aside></main>}
