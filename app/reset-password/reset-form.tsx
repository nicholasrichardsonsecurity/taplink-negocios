"use client";
import { FormEvent, useState } from "react";
export default function ResetPasswordForm({token}:{token:string}){const[error,setError]=useState("");const[done,setDone]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");const form=new FormData(event.currentTarget);const password=String(form.get("password")??""),confirmation=String(form.get("confirmation")??"");if(password!==confirmation){setError("As senhas não coincidem.");return}const response=await fetch("/api/auth/password-reset/confirm",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password})});if(response.ok){setDone(true);return}setError((await response.json().catch(()=>({error:"Não foi possível redefinir."}))).error)}return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span>T</span><b>TapLink</b></a><small>NOVA SENHA</small><h1>{done?"Senha redefinida.":"Crie uma nova senha."}</h1>{done?<><p>Todas as sessões anteriores foram encerradas.</p><a href="/login">Entrar novamente</a></>:<form onSubmit={submit}><label>Nova senha<input name="password" type="password" minLength={10} maxLength={128} autoComplete="new-password" required/></label><label>Confirmar senha<input name="confirmation" type="password" minLength={10} maxLength={128} autoComplete="new-password" required/></label>{error&&<div className="form-error">{error}</div>}<button>Redefinir senha</button></form>}</section><aside><small>PROTEÇÃO DE CONTA</small><h2>Uma troca.<br/>Nenhuma sessão antiga.</h2><p>O token expira e não pode ser reutilizado.</p></aside></main>}
